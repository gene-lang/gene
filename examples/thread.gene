#!/usr/bin/env gene run

(println "main thread")

(spawn
  (println "fire-and-forget worker")
)
(gene/sleep 50)

(var result
  (await
    (spawn_return
      (+ 40 2)
    )
  )
)

(println "spawn_return result:" result)
