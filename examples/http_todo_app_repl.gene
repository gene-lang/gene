#!/usr/bin/env gene run

(import * from "examples/http_todo_app")

(var port
  (if ($args/.size > 0)
    $args/0/.to_i
  else
    2080
  )
)

(var app ($tap (new App port)
  (/handler = (create_router))
))

($repl)
