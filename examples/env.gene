#!/usr/bin/env gene run

(println "HOME:" $env/HOME)
(println "USER:" $env/USER)

(set_env "CODEX_EXAMPLE_VALUE" "test")
(println "child process sees:" (system/exec "sh" "-c" "printf $CODEX_EXAMPLE_VALUE"))
