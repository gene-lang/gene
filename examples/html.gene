#!/usr/bin/env gene run

(import genex/html/tags/*)

(var doc
  (HTML
    (HEAD
      (TITLE "Gene HTML Example")
    )
    (BODY
      (DIV ^class "card"
        (H1 "Gene")
        (P "HTML generated from Gene.")
      )
    )
  )
)

(println doc/.to_s)
