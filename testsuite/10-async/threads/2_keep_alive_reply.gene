# Expected: 42

(do
  (gene/actor/enable ^workers 1)
  (var worker
    (gene/actor/spawn
      (fn [ctx msg state]
        (ctx .reply (+ msg 1))
        state)
    ))
  (println (await (worker .send_expect_reply 41)))
)
