# Expected: {^a 1 ^b 2}

(do
  (gene/actor/enable ^workers 1)
  (var worker
    (gene/actor/spawn
      (fn [ctx msg state]
        (ctx .reply msg)
        state)
    ))
  (println (await (worker .send_expect_reply {^a 1 ^b 2})))
)
