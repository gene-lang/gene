---
phase: 01.5-freezable-closures
plan: 02
subsystem: runtime
tags: [nim, runtime, freeze, closures, docs, shared-heap]
requires:
  - phase: 01.5-freezable-closures
    provides: closure freeze walker, deterministic capture paths, focused closure freeze unit tests
provides:
  - VM-level proof that frozen closures remain callable after the defining scope unwinds
  - cross-thread pointer publication coverage for frozen closure values with exact refcount restoration
  - explicit repo handoff from Phase 1.5 closure freezeability to Phase 2 actor consumption and Phase 4 thread-surface removal
affects: [Phase 2 actor runtime, closure sendability, shared-heap publication, migration documentation]
tech-stack:
  added: []
  patterns: [scope-teardown closure invocation proof, atomic closure publication slot, migration-boundary documentation]
key-files:
  created:
    - tests/integration/test_phase15_actor_ready_closures.nim
    - tests/test_phase15_shared_closure_heap.nim
    - .planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md
  modified:
    - docs/handbook/freeze.md
    - src/gene/stdlib/freeze.nim
key-decisions:
  - "Treat the namespace-valued self capture on VM-created closures as redundant metadata, not part of the freezable closure environment."
  - "Prove closure pointer-safety by publishing frozen VkFunction values through the same Atomic[uint64] slot pattern used for Phase 1 shared graphs."
  - "Document spawn/thread surfaces as migration boundaries only; Phase 2 consumes frozen closures, and Phase 4 retires the legacy thread-first API."
patterns-established:
  - "Returned frozen closures must be tested through the real VM call path after their defining scope has unwound."
  - "Closure shared-heap coverage should assert exact before/after refcount equality for the closure and its managed captures."
requirements-completed: [CLO-01]
duration: 37min
completed: 2026-04-19
---

# Phase 01.5 Plan 02: Actor-Ready Closure Proof Summary

**Frozen closures are now proven callable after scope teardown, proven pointer-shareable across threads without capture cloning, and explicitly handed off to Phase 2 while legacy thread-first surfaces remain unchanged until the later migration phase.**

## Performance

- **Duration:** 37 min
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Added a dedicated VM integration suite for frozen returned closures, including a `make_adder` shape and captured array/map/gene reads after scope teardown.
- Added a shared-heap stress test that publishes a frozen `VkFunction` through an atomic slot and verifies exact refcount restoration for the closure and its managed captures after worker-thread reads.
- Updated the freeze handbook so Phase 1.5 closure scope, Phase 2 actor ownership, and Phase 4 thread-surface retirement boundaries are explicit in repo docs.

## Task Commits

1. **Task 1: Prove frozen closures still execute after scope teardown** - `f165a25` (`fix`)
2. **Task 2: Add a shared-heap closure stress proof** - `94e69ce` (`test`)
3. **Task 3: Record the migration boundary and handoff explicitly** - `c94ef7f` (`docs`)

## Files Created/Modified

- `tests/integration/test_phase15_actor_ready_closures.nim` - Runtime proof for frozen returned closures after the capture scope has unwound.
- `tests/test_phase15_shared_closure_heap.nim` - Atomic publication and refcount-restoration proof for frozen closure values.
- `docs/handbook/freeze.md` - Explicit Phase 1.5 closure scope and migration-boundary handoff.
- `src/gene/stdlib/freeze.nim` - Narrow runtime unblock so VM-created closures ignore only redundant namespace-valued `self` captures during freeze validation.

## Decisions Made

- Keep the Phase 2 prerequisite focused on closure-freezeability and publication safety, not thread transport or actor scheduling.
- Preserve validation strictness for real captured values while exempting only the namespace-valued `self` slot that the VM already stores separately on `Function.ns`.
- Name the unchanged thread-first surfaces directly in the handbook so later phases do not infer API rewrite work into Phase 1.5.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] VM-created closures captured a redundant namespace `self` slot that blocked real freeze proofs**
- **Found during:** Task 1
- **Issue:** The new integration proof failed because closures created through the VM capture a namespace-valued `self` slot in `parent_scope`, and `freeze_value` rejected that `VkNamespace` capture even though the callable already stores its namespace on `Function.ns`.
- **Fix:** Updated `src/gene/stdlib/freeze.nim` to skip only namespace-valued `KEY_SELF` captures during validation while leaving all other capture kinds subject to the existing closure-freeze rule.
- **Files modified:** `src/gene/stdlib/freeze.nim`, `tests/integration/test_phase15_actor_ready_closures.nim`
- **Verification:** `nim c -r tests/integration/test_phase15_actor_ready_closures.nim`
- **Commit:** `f165a25`

**Total deviations:** 1 auto-fixed (`Rule 1`: 1)
**Impact:** Narrow runtime correction required to exercise real VM-created closures; no thread transport or actor scheduling surfaces were changed.

## Verification

- `nim c -r tests/integration/test_phase15_actor_ready_closures.nim`
- `nim c -r --mm:orc --threads:on tests/test_phase15_shared_closure_heap.nim`
- `nim c -r tests/integration/test_scope_lifetime.nim`
- `nim c -r tests/test_thread_msg.nim`
- `nimble test`
- `./testsuite/run_tests.sh`

## Known Stubs

None.

## Threat Flags

None.

## Next Phase Readiness

- Phase 2 can consume frozen closures through the same frozen-by-pointer contract as other shared graphs.
- The legacy thread-first concurrency surfaces remain documented and unchanged, so the later migration/removal work has an explicit repo handoff.

## Self-Check: PASSED

- Verified summary and implementation artifacts exist on disk.
- Verified task commits `f165a25`, `94e69ce`, and `c94ef7f` exist in git history.
