---
phase: 01.5-freezable-closures
plan: 02
type: execute
wave: 2
depends_on: [01.5-01]
files_modified:
  - tests/integration/test_phase15_actor_ready_closures.nim
  - tests/test_phase15_shared_closure_heap.nim
  - docs/handbook/freeze.md
autonomous: true
requirements: [CLO-01]
must_haves:
  truths:
    - "A frozen closure still executes correctly after the scope that created it has unwound."
    - "A frozen closure can be retained and observed across threads without cloning its captured environment or drifting refcounts."
    - "The repo-level handoff now states that Phase 2 consumes frozen closures while legacy thread-first surfaces remain intact until later migration phases."
  artifacts:
    - path: "tests/integration/test_phase15_actor_ready_closures.nim"
      provides: "VM-level execution proof for frozen closures after scope teardown"
      contains: "suite \"Phase 1.5 actor-ready closures\""
    - path: "tests/test_phase15_shared_closure_heap.nim"
      provides: "Cross-thread retain/release proof for frozen closure values"
      contains: "suite \"Phase 1.5 shared closure heap\""
    - path: "docs/handbook/freeze.md"
      provides: "Phase 1.5 closure scope plus explicit migration boundaries for Phase 2 and Phase 4"
      contains: "Phase 1.5"
  key_links:
    - from: "tests/integration/test_phase15_actor_ready_closures.nim"
      to: "src/gene/vm/exec_support.nim"
      via: "frozen closure invocation after defining scope teardown"
      pattern: "parent_scope|new_scope"
    - from: "tests/test_phase15_shared_closure_heap.nim"
      to: "tests/test_phase1_shared_heap.nim"
      via: "Atomic publication slot and exact before/after refcount equality"
      pattern: "Atomic\\[uint64\\]|ref_count"
    - from: "docs/handbook/freeze.md"
      to: "src/gene/vm/thread_native.nim"
      via: "documented migration boundary only; no code edits"
      pattern: "Thread.send|spawn|GENE_MAX_THREADS"
---

<objective>
Prove that the new closure-freeze rule is actor-ready in practice and record
the migration boundary explicitly: Phase 2 may consume frozen closures, while
legacy thread-first concurrency surfaces remain visible but unchanged until
later phases.

Purpose: Finish Phase 1.5 with runtime evidence and a durable handoff to the
actor migration sequence.
Output: Integration/runtime proofs plus updated freeze handbook guidance.
</objective>

<execution_context>
@/Users/gcao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gcao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.5-freezable-closures/01.5-RESEARCH.md
@.planning/phases/01.5-freezable-closures/01.5-PATTERNS.md
@.planning/phases/01.5-freezable-closures/01.5-VALIDATION.md
@.planning/phases/01-deep-frozen-bit-shared-heap-freeze/01-05-SUMMARY.md
@.planning/phases/01.5-freezable-closures/01.5-01-PLAN.md
@tests/test_phase1_shared_heap.nim
@tests/integration/test_scope_lifetime.nim
@tests/integration/test_function_optimization.nim
@docs/handbook/freeze.md
@src/gene/vm/thread_native.nim
@src/gene/serdes.nim

<interfaces>
From tests/test_phase1_shared_heap.nim:
```nim
type SharedSlot = object
  raw: Atomic[uint64]

proc ref_count_of(v: Value): int
proc worker_read(job: ptr WorkerJob) {.thread, gcsafe.}
```

From tests/integration/test_scope_lifetime.nim:
```nim
test_vm """
  (fn test_scope_teardown_after_async_capture []
    (var f
      (do
        (var payload [5 6 7])
        (var bonus [9])
        (async (+ (./ payload 2) (./ bonus 0)))))
    (await f))
  (test_scope_teardown_after_async_capture)
""", 16
```

From docs/handbook/freeze.md:
```md
- closures are deferred to Phase 1.5
```

From src/gene/vm/thread_native.nim:
```nim
thread_class.def_native_method("send", ...)
thread_class.def_native_method("send_expect_reply", ...)
proc thread_on_message(...)
```
</interfaces>
</context>

<source_audit>
SOURCE   | ID     | Feature / Constraint | Plan | Status | Notes
-------- | ------ | -------------------- | ---- | ------ | -----
GOAL     | —      | Freeze closures with freezable captured environments so they become pointer-shareable prerequisites for Phase 2 | 01, 02 | COVERED | 02 adds runtime evidence and handoff documentation
REQ      | CLO-01 | Closures with freezable captured environments can be frozen and sent by pointer across actor boundaries | 01, 02 | COVERED | 02 verifies pointer-safe sharing and callable behavior
RESEARCH | R-03   | Leave `serialize_literal` and legacy thread transport unchanged in Phase 1.5 | 02 | COVERED | Documented explicitly with no code edits to those modules
RESEARCH | R-04   | Add runtime proof after scope unwind and shared-pointer verification | 02 | COVERED | This plan is the proof plan
CONTEXT  | U-01   | Plan only closure-freeze prerequisite work | 02 | COVERED | No scheduler or transport rewrite work
CONTEXT  | U-02   | Keep thread-first surfaces visible as migration boundaries only | 02 | COVERED | Handbook update names them directly
CONTEXT  | U-03   | Make Phase 2 and later thread-surface removal handoff explicit | 02 | COVERED | Dedicated documentation acceptance criteria
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Prove frozen closures still execute after scope teardown</name>
  <files>tests/integration/test_phase15_actor_ready_closures.nim</files>
  <read_first>
@tests/integration/test_scope_lifetime.nim
@tests/integration/test_function_optimization.nim
@src/gene/vm/exec.nim
@src/gene/vm/exec_support.nim
@src/gene/stdlib/freeze.nim
  </read_first>
  <action>Create `tests/integration/test_phase15_actor_ready_closures.nim` as the VM-level proof that the new closure-freeze rule survives real execution. Add Gene programs that define a closure inside a short-lived inner scope, freeze it before that scope unwinds, then invoke it later and assert captured arrays/maps/genes still read correctly. Include at least one returned-closure shape that mirrors the disabled closure examples in `tests/integration/test_function_optimization.nim`, but keep the Phase 1.5 proof isolated in this new file rather than reworking the broader performance suite. Keep this task focused on callable correctness after scope teardown; do not introduce actor mailboxes or thread transport changes.</action>
  <verify>
    <automated>nim c -r tests/integration/test_phase15_actor_ready_closures.nim</automated>
  </verify>
  <acceptance_criteria>
    - The new integration suite passes by invoking frozen closures through the existing VM call path.
    - At least one test proves a closure created in an inner scope still works after that scope has unwound.
    - No legacy thread API files are edited for this proof.
  </acceptance_criteria>
  <done>There is a dedicated integration proof that frozen closures remain callable after the capture site's scope lifetime ends.</done>
</task>

<task type="auto">
  <name>Task 2: Add a shared-heap closure stress proof</name>
  <files>tests/test_phase15_shared_closure_heap.nim</files>
  <read_first>
@tests/test_phase1_shared_heap.nim
@src/gene/stdlib/freeze.nim
@src/gene/types/type_defs.nim
@tests/test_phase15_freezable_closures.nim
  </read_first>
  <action>Create `tests/test_phase15_shared_closure_heap.nim` by mirroring the publication and refcount-check pattern from `tests/test_phase1_shared_heap.nim`, but make the published root a frozen `VkFunction` with captured frozen data. Publish the closure pointer through an `Atomic[uint64]`, retain/release it from worker threads, and assert exact before/after refcount equality for the closure plus its captured managed graph. The worker threads may inspect frozen capture contents or closure metadata, but do not retrofit cross-thread invocation semantics into the legacy thread runtime; the purpose is pointer-safety of the closure value and its environment, not actor scheduling or `Thread.send` behavior.</action>
  <verify>
    <automated>nim c -r --mm:orc --threads:on tests/test_phase15_shared_closure_heap.nim</automated>
  </verify>
  <acceptance_criteria>
    - The test proves frozen closure values can be published by pointer and retained from other threads without cloning their captures.
    - Refcounts for the closure and its captured graph return to the exact pre-worker baseline after all worker joins.
    - The test does not modify `src/gene/vm/thread_native.nim` or `src/gene/serdes.nim`.
  </acceptance_criteria>
  <done>Phase 1.5 has the same class of cross-thread pointer-safety evidence for closures that Phase 1 produced for frozen container graphs.</done>
</task>

<task type="auto">
  <name>Task 3: Record the migration boundary and handoff explicitly</name>
  <files>docs/handbook/freeze.md</files>
  <read_first>
@docs/handbook/freeze.md
@.planning/ROADMAP.md
@src/gene/vm/thread_native.nim
@src/gene/serdes.nim
  </read_first>
  <action>Update `docs/handbook/freeze.md` so the Phase 1.5 section explains the new closure scope precisely: `VkFunction` closures with freezable captured environments are now part of the frozen surface, while `VkBlock`, classes, bound methods, native resources, and legacy thread-first concurrency surfaces remain outside this phase. Name the migration-boundary surfaces explicitly: `spawn`, `spawn_return`, `Thread.send`, `Thread.send_expect_reply`, `Thread.on_message`, `keep_alive`, and `GENE_MAX_THREADS`. State that Phase 2 will consume frozen closures in the actor runtime and that the eventual removal/retirement of thread-first surfaces belongs to the later migration phase, not here.</action>
  <verify>
    <automated>bash -lc 'rg -q "spawn" docs/handbook/freeze.md && rg -q "spawn_return" docs/handbook/freeze.md && rg -q "Thread\\.send" docs/handbook/freeze.md && rg -q "Thread\\.send_expect_reply" docs/handbook/freeze.md && rg -q "Thread\\.on_message" docs/handbook/freeze.md && rg -q "keep_alive" docs/handbook/freeze.md && rg -q "GENE_MAX_THREADS" docs/handbook/freeze.md && rg -q "Phase 2" docs/handbook/freeze.md && rg -Eq "Phase 4|later migration phase|later phases" docs/handbook/freeze.md && rg -q "closures" docs/handbook/freeze.md'</automated>
  </verify>
  <acceptance_criteria>
    - The handbook explicitly distinguishes Phase 1.5 closure support from Phase 2 actor runtime work.
    - The named legacy thread surfaces are listed as migration boundaries, not rewritten API work.
    - The handoff to later thread-surface removal is documented in the repo, not just in plan prose.
  </acceptance_criteria>
  <done>The repo carries an explicit handoff note that keeps Phase 1.5 scoped to closure freezeability while making later actor/thread migration work obvious.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen closure value → cross-thread publication slot | Incorrect publication invariants could expose mutable captures across threads |
| Runtime docs → later migration phases | Ambiguous handoff wording could lead a later executor to rewrite legacy thread APIs in the wrong phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-04 | T | `tests/test_phase15_shared_closure_heap.nim` | mitigate | Mirror the exact-before/after refcount proof from Phase 1 shared-heap coverage and keep worker behavior read-only |
| T-01.5-05 | D | Frozen closure execution after scope unwind | mitigate | Add dedicated integration tests that invoke frozen closures after their defining scope has unwound |
| T-01.5-06 | R | Phase boundary documentation | mitigate | Name the unchanged legacy thread surfaces directly in `docs/handbook/freeze.md` and point to Phase 2 / Phase 4 ownership |
</threat_model>

<verification>
- `nim c -r tests/integration/test_phase15_actor_ready_closures.nim`
- `nim c -r --mm:orc --threads:on tests/test_phase15_shared_closure_heap.nim`
- `nim c -r tests/integration/test_scope_lifetime.nim`
- `nim c -r tests/test_thread_msg.nim`
- `nimble test`
- `./testsuite/run_tests.sh`
</verification>

<success_criteria>
- Frozen closures are proven callable after scope teardown.
- Frozen closures are proven pointer-shareable across threads with exact refcount restoration.
- The handbook now states that Phase 2 consumes closure freezeability while legacy thread surfaces stay unchanged until later migration/removal phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md`
</output>
