---
phase: 00-unify-lifetime-and-publication-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gene/types/core/value_ops.nim
  - src/gene/types/memory.nim
  - src/gene/types/core/frames.nim
  - src/gene/native/trampoline.nim
  - src/gene/vm/exec.nim
  - tests/integration/test_scope_lifetime.nim
  - tests/test_native_trampoline.nim
autonomous: true
requirements:
  - LIFE-01
must_haves:
  truths:
    - "Managed Value lifetimes are governed by one ownership model across manual VM writes, Nim assignment hooks, and native trampoline boundaries"
    - "Scope lifetime and native trampoline tests stay green after the RC refactor"
    - "Manual retain/release calls that duplicate managed-hook ownership accounting are removed or routed through the same primitives"
  artifacts:
    - path: "src/gene/types/memory.nim"
      provides: "Canonical retain/release lifecycle rules for managed Values"
    - path: "src/gene/types/core/value_ops.nim"
      provides: "Manual helpers aligned with the canonical ownership model"
    - path: "tests/integration/test_scope_lifetime.nim"
      provides: "Regression coverage for scope and async ownership behavior"
  key_links:
    - from: "src/gene/types/core/value_ops.nim"
      to: "src/gene/types/memory.nim"
      via: "Manual helpers use the same lifetime rules as managed hooks"
    - from: "src/gene/native/trampoline.nim"
      to: "src/gene/types/memory.nim"
      via: "Descriptor/callable cleanup follows canonical release semantics"
---

<objective>
Unify ref-counting paths around the managed `Value` hook model.

Purpose: Remove the mixed ownership semantics that currently let manual
`retain` / `release` logic and Nim `=copy` / `=destroy` hooks double-account the
same lifetime boundaries.
Output: Canonical ownership rules in `memory.nim`, aligned helpers in
`value_ops.nim`, and regression coverage for scope/async/native boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-unify-lifetime-and-publication-semantics/00-CONTEXT.md
@src/gene/types/core/value_ops.nim
@src/gene/types/memory.nim
@src/gene/types/core/frames.nim
@src/gene/native/trampoline.nim
@src/gene/vm/exec.nim
@tests/integration/test_scope_lifetime.nim
@tests/test_native_trampoline.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock current ownership boundaries with targeted tests</name>
  <files>tests/integration/test_scope_lifetime.nim, tests/test_native_trampoline.nim</files>
  <read_first>tests/integration/test_scope_lifetime.nim, tests/test_native_trampoline.nim, src/gene/types/memory.nim</read_first>
  <action>Extend existing scope-lifetime and native-trampoline tests so they pin the ownership boundaries touched by this refactor. Cover at least: nested scope teardown, async closure capture, and native descriptor cleanup or repeated native readiness across repeated execution. Use the existing test style rather than inventing a parallel harness.</action>
  <verify>nim c -r tests/integration/test_scope_lifetime.nim && nim c -r tests/test_native_trampoline.nim</verify>
  <acceptance_criteria>
    - tests/integration/test_scope_lifetime.nim contains at least one case exercising async or nested-scope lifetime after the refactor target is identified
    - tests/test_native_trampoline.nim still asserts `native_ready` behavior at the function boundary
  </acceptance_criteria>
  <done>Regression tests fail on a broken ownership refactor and pass on the intended behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse RC ownership onto one source of truth</name>
  <files>src/gene/types/core/value_ops.nim, src/gene/types/memory.nim, src/gene/types/core/frames.nim, src/gene/native/trampoline.nim, src/gene/vm/exec.nim</files>
  <read_first>src/gene/types/core/value_ops.nim, src/gene/types/memory.nim, src/gene/types/core/frames.nim, src/gene/native/trampoline.nim, src/gene/vm/exec.nim</read_first>
  <action>Make `src/gene/types/memory.nim` the canonical ownership model for managed `Value` lifetimes. Audit direct `retain(` / `release(` use in `value_ops.nim`, frame cleanup, native trampoline descriptor handling, and VM write sites that currently rely on manual accounting. Remove or reroute any call sites that duplicate the same assignment boundary already covered by `=copy`, `=destroy`, or `=sink`. Do not change lock-release calls in unrelated threading/logging modules; this plan only touches managed `Value` ownership.</action>
  <verify>rg -n \"retainManaged|releaseManaged|proc =copy|proc =destroy|proc =sink\" src/gene/types/memory.nim && nim c -r tests/integration/test_scope_lifetime.nim</verify>
  <acceptance_criteria>
    - memory.nim remains the single file defining managed-hook lifecycle behavior
    - changed manual ownership sites now document or enforce why they still need explicit retain/release
    - no new duplicate retain/release pair is introduced at a boundary already covered by managed hooks
  </acceptance_criteria>
  <done>Manual and managed ownership rules no longer disagree about who owns a managed Value at assignment/store boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Verify RC behavior at runtime boundaries</name>
  <files>tests/integration/test_scope_lifetime.nim, tests/test_native_trampoline.nim</files>
  <read_first>tests/integration/test_scope_lifetime.nim, tests/test_native_trampoline.nim</read_first>
  <action>Run the targeted regression suite and inspect failures for ownership drift. If a failure points to a remaining mixed-ownership path, fix that path before closing the plan instead of papering over it in tests.</action>
  <verify>nim c -r tests/integration/test_scope_lifetime.nim && nim c -r tests/test_native_trampoline.nim</verify>
  <acceptance_criteria>
    - both targeted test commands pass
    - no failing assertion is left attributed to RC behavior
  </acceptance_criteria>
  <done>The runtime survives the targeted ownership regression suite with a single ownership model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] nim c -r tests/integration/test_scope_lifetime.nim
- [ ] nim c -r tests/test_native_trampoline.nim
- [ ] Manual ownership sites touched by the diff have a coherent reason to remain explicit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Lifetime behavior is grounded in one canonical ownership model
</success_criteria>

<output>
After completion, create `.planning/phases/00-unify-lifetime-and-publication-semantics/00-01-SUMMARY.md`
</output>
