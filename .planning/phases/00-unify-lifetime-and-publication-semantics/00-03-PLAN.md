---
phase: 00-unify-lifetime-and-publication-semantics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gene/vm/async_exec.nim
  - src/gene/vm/thread_native.nim
  - tests/integration/test_thread.nim
autonomous: true
requirements:
  - THR-01
  - THR-02
must_haves:
  truths:
    - "Thread reply polling uses the caller's thread slot instead of hard-coding thread 0"
    - "Thread.on_message installs callbacks on the target thread VM"
    - "Existing thread send/reply tests remain green after the bug fixes"
  artifacts:
    - path: "src/gene/vm/async_exec.nim"
      provides: "Correct thread reply polling behavior"
    - path: "src/gene/vm/thread_native.nim"
      provides: "Target-VM callback registration for `.on_message`"
    - path: "tests/integration/test_thread.nim"
      provides: "Regression coverage for thread reply and callback behavior"
  key_links:
    - from: "src/gene/vm/async_exec.nim"
      to: "tests/integration/test_thread.nim"
      via: "Thread reply polling fixes are exercised by integration tests"
    - from: "src/gene/vm/thread_native.nim"
      to: "tests/integration/test_thread.nim"
      via: "Target-VM callback registration is asserted by `.on_message` tests"
---

<objective>
Repair thread reply polling and target-VM callback registration.

Purpose: Fix the two known thread-runtime correctness bugs before actor work
builds on top of the current mailbox/message infrastructure.
Output: Correct per-thread reply polling, target-thread `.on_message`
registration, and regression tests covering both fixes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-unify-lifetime-and-publication-semantics/00-CONTEXT.md
@src/gene/vm/async_exec.nim
@src/gene/vm/thread_native.nim
@tests/integration/test_thread.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for the two thread runtime bugs</name>
  <files>tests/integration/test_thread.nim</files>
  <read_first>tests/integration/test_thread.nim, src/gene/vm/async_exec.nim, src/gene/vm/thread_native.nim</read_first>
  <action>Extend `tests/integration/test_thread.nim` so it fails when `poll_event_loop` only drains thread 0 or when `.on_message` registers on the caller VM instead of the target thread. Reuse the existing `send_expect_reply` and keep-alive patterns rather than inventing a separate harness.</action>
  <verify>nim c -r tests/integration/test_thread.nim</verify>
  <acceptance_criteria>
    - tests/integration/test_thread.nim contains at least one case that would fail if replies are only drained from thread 0
    - tests/integration/test_thread.nim contains at least one case that would fail if `.on_message` is stored on the caller VM
  </acceptance_criteria>
  <done>Thread regression tests make both runtime bugs reproducible before the fix lands.</done>
</task>

<task type="auto">
  <name>Task 2: Fix per-thread reply polling and target-VM callback ownership</name>
  <files>src/gene/vm/async_exec.nim, src/gene/vm/thread_native.nim</files>
  <read_first>src/gene/vm/async_exec.nim, src/gene/vm/thread_native.nim, tests/integration/test_thread.nim</read_first>
  <action>In `src/gene/vm/async_exec.nim`, replace the hard-coded `THREAD_DATA[0].channel` polling path with the caller's current thread slot while preserving future completion semantics. In `src/gene/vm/thread_native.nim`, route `.on_message` registration to the target thread's VM (for example via a control message or target-owned registry update) instead of mutating the caller VM directly.</action>
  <verify>rg -n \"THREAD_DATA\\[0\\]|message_callbacks|on_message\" src/gene/vm/async_exec.nim src/gene/vm/thread_native.nim && nim c -r tests/integration/test_thread.nim</verify>
  <acceptance_criteria>
    - async_exec.nim no longer hard-codes thread 0 for reply polling
    - thread_native.nim no longer appends `.on_message` callbacks directly to the caller VM for target-thread registration
    - thread integration tests pass after the fix
  </acceptance_criteria>
  <done>Replies and callbacks are handled by the intended thread runtime rather than accidentally by the main/calling VM.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run thread behavior checks</name>
  <files>tests/integration/test_thread.nim</files>
  <read_first>tests/integration/test_thread.nim</read_first>
  <action>Run the thread integration suite and inspect failures for remaining cross-thread ownership or routing assumptions. Fix the underlying runtime behavior before closing the plan.</action>
  <verify>nim c -r tests/integration/test_thread.nim</verify>
  <acceptance_criteria>
    - tests/integration/test_thread.nim passes
    - no remaining failing thread routing assertion is deferred out of the plan
  </acceptance_criteria>
  <done>The thread runtime regressions are fixed and covered by integration tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] nim c -r tests/integration/test_thread.nim
- [ ] Reply polling references the active thread slot, not thread 0
- [ ] `.on_message` registration is owned by the target thread VM
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Thread reply and callback behavior is correct on non-main paths
</success_criteria>

<output>
After completion, create `.planning/phases/00-unify-lifetime-and-publication-semantics/00-03-SUMMARY.md`
</output>
