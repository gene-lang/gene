---
phase: 00-unify-lifetime-and-publication-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - src/gene/compiler.nim
  - src/gene/gir.nim
  - src/gene/vm/exec.nim
  - src/gene/vm/native.nim
  - tests/integration/test_cli_gir.nim
  - tests/test_native_trampoline.nim
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
must_haves:
  truths:
    - "Compiled function/block bodies are not published through unsynchronized writes"
    - "Inline caches are sized or synchronized before execution instead of growing opportunistically in the hot path"
    - "Native entry publication does not expose `native_entry` before `native_ready` is safely visible"
  artifacts:
    - path: "src/gene/vm/exec.nim"
      provides: "Publication-safe body and inline-cache consumption"
    - path: "src/gene/vm/native.nim"
      provides: "Publication-safe native entry initialization"
    - path: "tests/integration/test_cli_gir.nim"
      provides: "Regression coverage for lazy compile / GIR load behavior"
  key_links:
    - from: "src/gene/compiler.nim"
      to: "src/gene/vm/exec.nim"
      via: "Compiler/runtime agree on inline cache sizing and compiled body publication"
    - from: "src/gene/vm/native.nim"
      to: "tests/test_native_trampoline.nim"
      via: "Native publication invariants are asserted by trampoline tests"
---

<objective>
Fix lazy publication for compiled bodies, inline caches, and native entry state.

Purpose: Eliminate the race-prone publication points that later shared-heap and
actor work would otherwise inherit unchanged.
Output: Publication-safe compiler/runtime handoff for `body_compiled`,
`inline_caches`, and `native_ready` / `native_entry`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-unify-lifetime-and-publication-semantics/00-CONTEXT.md
@src/gene/compiler.nim
@src/gene/gir.nim
@src/gene/vm/exec.nim
@src/gene/vm/native.nim
@tests/integration/test_cli_gir.nim
@tests/test_native_trampoline.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock current lazy publication behavior with regression coverage</name>
  <files>tests/integration/test_cli_gir.nim, tests/test_native_trampoline.nim</files>
  <read_first>tests/integration/test_cli_gir.nim, tests/test_native_trampoline.nim, src/gene/compiler.nim, src/gene/vm/native.nim</read_first>
  <action>Extend existing GIR and native trampoline tests so they pin the publication boundaries touched here. Cover first-use compilation, loaded GIR execution, and repeated native readiness checks. Tests should fail if publication becomes torn, duplicated, or dependent on the old hot-path cache-growth behavior.</action>
  <verify>nim c -r tests/integration/test_cli_gir.nim && nim c -r tests/test_native_trampoline.nim</verify>
  <acceptance_criteria>
    - tests/integration/test_cli_gir.nim covers a first-use compile/load path that touches `body_compiled`
    - tests/test_native_trampoline.nim still checks `native_ready` and `native_entry`
  </acceptance_criteria>
  <done>Publication regressions are observable before the runtime changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unsynchronized publication paths</name>
  <files>src/gene/compiler.nim, src/gene/gir.nim, src/gene/vm/exec.nim, src/gene/vm/native.nim</files>
  <read_first>src/gene/compiler.nim, src/gene/gir.nim, src/gene/vm/exec.nim, src/gene/vm/native.nim</read_first>
  <action>Normalize publication around a single safe pattern. Replace the `inline_cache(cu, pc)` fallback growth in `src/gene/vm/exec.nim` with an invariant that caches are sized before execution or synchronized when published. Route `Function.body_compiled` / `Block.body_compiled` publication through a guarded helper or eager-initialization path instead of scattered unsynchronized writes. In `src/gene/vm/native.nim`, ensure `native_entry` is assigned before the publish flag becomes visible, and make the ready check safe for concurrent first use.</action>
  <verify>rg -n \"inline_caches|body_compiled|native_ready|native_entry\" src/gene/compiler.nim src/gene/gir.nim src/gene/vm/exec.nim src/gene/vm/native.nim && nim c -r tests/test_native_trampoline.nim</verify>
  <acceptance_criteria>
    - exec.nim no longer contains opportunistic `while cu.inline_caches.len <= pc` growth in the execution hot path
    - changed `body_compiled` writes flow through one synchronized or eagerly-initialized publication path
    - native publication writes `native_entry` and readiness state in an order that cannot expose a nil/tearing entry after ready is observed
  </acceptance_criteria>
  <done>The runtime no longer relies on unsynchronized publication for compiled bodies, inline caches, or native entry state.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify compile, load, and native execution paths</name>
  <files>tests/integration/test_cli_gir.nim, tests/test_native_trampoline.nim</files>
  <read_first>tests/integration/test_cli_gir.nim, tests/test_native_trampoline.nim</read_first>
  <action>Run the targeted GIR and native publication suite. If a failure exposes a publication-ordering assumption elsewhere in the runtime, fix that publication site before closing the plan.</action>
  <verify>nim c -r tests/integration/test_cli_gir.nim && nim c -r tests/test_native_trampoline.nim</verify>
  <acceptance_criteria>
    - both targeted publication test commands pass
    - no remaining publication failure is waived or deferred inside the plan
  </acceptance_criteria>
  <done>Compile, GIR load, and native execution paths all honor the new publication invariants.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] nim c -r tests/integration/test_cli_gir.nim
- [ ] nim c -r tests/test_native_trampoline.nim
- [ ] Publication order for `body_compiled`, `inline_caches`, and native entry is explicit in the implementation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Lazy publication hazards are removed from the targeted runtime paths
</success_criteria>

<output>
After completion, create `.planning/phases/00-unify-lifetime-and-publication-semantics/00-02-SUMMARY.md`
</output>
