---
phase: 00-unify-lifetime-and-publication-semantics
plan: 04
type: execute
wave: 3
depends_on:
  - "00-01"
files_modified:
  - src/gene/stdlib/strings.nim
  - src/gene/stdlib/core.nim
  - src/gene/vm/exec.nim
  - tests/integration/test_stdlib_string.nim
autonomous: true
requirements:
  - STR-01
  - STR-02
must_haves:
  truths:
    - "`String.append` returns a new string instead of mutating shared storage in place"
    - "`IkPushValue` no longer copies string literals defensively on push"
    - "Existing string stdlib behavior remains green aside from the intentional append-semantics cut"
  artifacts:
    - path: "src/gene/stdlib/strings.nim"
      provides: "Immutable string method behavior"
    - path: "src/gene/stdlib/core.nim"
      provides: "Core helper parity with immutable string behavior"
    - path: "tests/integration/test_stdlib_string.nim"
      provides: "Regression coverage for immutable string semantics"
  key_links:
    - from: "src/gene/stdlib/strings.nim"
      to: "src/gene/vm/exec.nim"
      via: "Immutable string semantics make literal push copies unnecessary"
    - from: "src/gene/stdlib/core.nim"
      to: "tests/integration/test_stdlib_string.nim"
      via: "Core helper behavior matches the class-based string API"
---

<objective>
Cut over to immutable strings and delete literal push copies.

Purpose: Remove the runtime's mutable-string special case before actor/shared
heap work depends on string pointer sharing.
Output: Return-new-string semantics for `String.append`, parity across stdlib
helpers, and a hot path with no defensive literal copy in `IkPushValue`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-unify-lifetime-and-publication-semantics/00-CONTEXT.md
@src/gene/stdlib/strings.nim
@src/gene/stdlib/core.nim
@src/gene/vm/exec.nim
@tests/integration/test_stdlib_string.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand string regression coverage around the immutability cut</name>
  <files>tests/integration/test_stdlib_string.nim</files>
  <read_first>tests/integration/test_stdlib_string.nim, src/gene/stdlib/strings.nim, src/gene/stdlib/core.nim</read_first>
  <action>Add targeted tests for the behavior that changes here: `String.append` returning a new value, alias safety between the original and appended string, and any helper surface in `stdlib/core.nim` that still assumes in-place mutation. Keep the rest of the existing string suite intact.</action>
  <verify>nim c -r tests/integration/test_stdlib_string.nim</verify>
  <acceptance_criteria>
    - tests/integration/test_stdlib_string.nim contains a case that distinguishes in-place append from return-new semantics
    - the test suite still covers the existing broad string surface after the new cases are added
  </acceptance_criteria>
  <done>The string suite makes the intended immutable-string cut explicit before implementation changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement immutable string semantics</name>
  <files>src/gene/stdlib/strings.nim, src/gene/stdlib/core.nim, src/gene/vm/exec.nim</files>
  <read_first>src/gene/stdlib/strings.nim, src/gene/stdlib/core.nim, src/gene/vm/exec.nim, tests/integration/test_stdlib_string.nim</read_first>
  <action>Change `String.append` (and the mirrored helper path in `src/gene/stdlib/core.nim`) to return a new string rather than mutating the receiver in place. Remove the mutable-string assumptions and helper comments that justify them. Once strings are immutable, delete the `IkPushValue` branch that copies string literals defensively before pushing them on the VM stack.</action>
  <verify>rg -n \"String.append|new_str_value\\(inst.arg0.str\\)|ensure_mutable_string\" src/gene/stdlib/strings.nim src/gene/stdlib/core.nim src/gene/vm/exec.nim && nim c -r tests/integration/test_stdlib_string.nim</verify>
  <acceptance_criteria>
    - strings.nim and core.nim no longer rely on in-place `append` mutation
    - exec.nim no longer contains the string-literal defensive copy inside `IkPushValue`
    - the string integration suite passes after the cut
  </acceptance_criteria>
  <done>Strings are immutable at the runtime surface and the VM no longer compensates for mutable literal sharing.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify string behavior after the cut</name>
  <files>tests/integration/test_stdlib_string.nim</files>
  <read_first>tests/integration/test_stdlib_string.nim</read_first>
  <action>Run the string integration suite and inspect any regression against the intentional API break. Fix code or tests until the only behavior change is the approved append semantics.</action>
  <verify>nim c -r tests/integration/test_stdlib_string.nim</verify>
  <acceptance_criteria>
    - tests/integration/test_stdlib_string.nim passes
    - no unrelated string regression is deferred out of the plan
  </acceptance_criteria>
  <done>The string stdlib surface is stable after the immutability cut.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] nim c -r tests/integration/test_stdlib_string.nim
- [ ] `IkPushValue` no longer copies string literals
- [ ] `String.append` returns a new string value
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Immutable string semantics are enforced across stdlib and VM literal loading
</success_criteria>

<output>
After completion, create `.planning/phases/00-unify-lifetime-and-publication-semantics/00-04-SUMMARY.md`
</output>
