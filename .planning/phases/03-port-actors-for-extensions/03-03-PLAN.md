---
phase: 03-port-actors-for-extensions
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/genex/http.nim
  - src/genex/ai/bindings.nim
  - tests/integration/test_http.nim
  - tests/integration/test_ai_scheduler.nim
  - tests/integration/test_ai_slack_socket_mode.nim
autonomous: true
requirements: [ACT-03]
---

<objective>
Move HTTP and AI extension concurrency onto registered port boundaries so they stop owning extension-local worker pools or process-global callback state outside the actor runtime.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock HTTP and AI port-boundary regressions</name>
  <files>tests/integration/test_http.nim, tests/integration/test_ai_scheduler.nim, tests/integration/test_ai_slack_socket_mode.nim</files>
  <action>Extend the existing HTTP and AI integration suites so they assert the port-boundary behavior rather than the old extension-local worker/global-callback model.</action>
  <verify><automated>nim c -r tests/integration/test_http.nim && nim c -r tests/integration/test_ai_scheduler.nim && nim c -r tests/integration/test_ai_slack_socket_mode.nim</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Migrate HTTP and AI ingress ownership onto ports</name>
  <files>src/genex/http.nim, src/genex/ai/bindings.nim</files>
  <action>Replace the HTTP Gene-thread worker pool and AI global callback ownership with registered port-backed ownership using the Phase 3 extension substrate.</action>
  <verify><automated>nim c -r tests/integration/test_http.nim && nim c -r tests/integration/test_ai_scheduler.nim && nim c -r tests/integration/test_ai_slack_socket_mode.nim</automated></verify>
</task>
</tasks>

<verification>
- `nim c -r tests/integration/test_http.nim`
- `nim c -r tests/integration/test_ai_scheduler.nim`
- `nim c -r tests/integration/test_ai_slack_socket_mode.nim`
</verification>

<output>
After completion, create `.planning/phases/03-port-actors-for-extensions/03-03-SUMMARY.md`
</output>
