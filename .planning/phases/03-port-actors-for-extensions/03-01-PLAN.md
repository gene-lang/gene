---
phase: 03-port-actors-for-extensions
plan: 01
type: execute
wave: 1
files_modified:
  - src/gene/vm/extension_abi.nim
  - src/gene/vm/extension.nim
  - src/gene/vm/actor.nim
  - src/gene/types/type_defs.nim
  - tests/integration/test_extension_ports.nim
autonomous: true
requirements: [ACT-03]
---

<objective>
Define one extension-port registration substrate that materializes singleton, pool, and factory ports as actor-backed runtime boundaries.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add integration coverage for extension port registration</name>
  <files>tests/integration/test_extension_ports.nim</files>
  <action>Create a new integration suite that proves singleton, pool, and factory port definitions register through one runtime-owned API and materialize as actor-backed handles.</action>
  <verify><automated>nim c -r tests/integration/test_extension_ports.nim</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Add port registration/materialization to the extension runtime</name>
  <files>src/gene/vm/extension_abi.nim, src/gene/vm/extension.nim, src/gene/vm/actor.nim, src/gene/types/type_defs.nim</files>
  <action>Extend the extension ABI/bootstrap path with singleton, pool, and factory registration primitives that materialize actor-backed port handles through the completed Phase 2 actor runtime.</action>
  <verify><automated>nim c -r tests/integration/test_extension_ports.nim</automated></verify>
</task>
</tasks>

<verification>
- `nim c -r tests/integration/test_extension_ports.nim`
</verification>

<output>
After completion, create `.planning/phases/03-port-actors-for-extensions/03-01-SUMMARY.md`
</output>
