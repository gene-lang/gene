---
phase: 03-port-actors-for-extensions
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - docs/http_server_and_client.md
  - docs/handbook/actors.md
  - docs/thread_support.md
  - tests/integration/test_thread.nim
autonomous: true
requirements: [ACT-03]
---

<objective>
Close Phase 3 by documenting the extension/port boundary and verifying migrated extensions without prematurely removing the legacy thread API.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Publish extension port migration guidance</name>
  <files>docs/http_server_and_client.md, docs/handbook/actors.md, docs/thread_support.md</files>
  <action>Update docs so HTTP/AI/LLM ownership is described through port boundaries while the thread docs stay explicit that Phase 4 still owns actual thread-API removal.</action>
  <verify><automated>rg -n \"port|Phase 4|thread compatibility|legacy thread\" docs/http_server_and_client.md docs/handbook/actors.md docs/thread_support.md</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Run Phase 3 closeout verification</name>
  <files>tests/integration/test_extension_ports.nim, tests/integration/test_llm_mock.nim, tests/integration/test_http.nim, tests/integration/test_ai_scheduler.nim, tests/integration/test_ai_slack_socket_mode.nim, tests/integration/test_thread.nim</files>
  <action>Run the targeted extension migration sweep plus the legacy thread compatibility suite so Phase 3 closes with evidence that extensions now respect actor/port boundaries without prematurely removing the thread lane.</action>
  <verify><automated>nim c -r tests/integration/test_extension_ports.nim && nim c -r tests/integration/test_llm_mock.nim && nim c -r tests/integration/test_http.nim && nim c -r tests/integration/test_ai_scheduler.nim && nim c -r tests/integration/test_ai_slack_socket_mode.nim && nim c -r tests/integration/test_thread.nim</automated></verify>
</task>
</tasks>

<verification>
- `nim c -r tests/integration/test_extension_ports.nim`
- `nim c -r tests/integration/test_llm_mock.nim`
- `nim c -r tests/integration/test_http.nim`
- `nim c -r tests/integration/test_ai_scheduler.nim`
- `nim c -r tests/integration/test_ai_slack_socket_mode.nim`
- `nim c -r tests/integration/test_thread.nim`
</verification>

<output>
After completion, create `.planning/phases/03-port-actors-for-extensions/03-04-SUMMARY.md`
</output>
