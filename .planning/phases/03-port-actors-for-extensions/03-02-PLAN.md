---
phase: 03-port-actors-for-extensions
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/genex/llm.nim
  - tests/integration/test_llm_mock.nim
autonomous: true
requirements: [ACT-03]
---

<objective>
Migrate `genex/llm` onto the singleton-port substrate so backend ownership is actor-bound instead of lock-bound.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend mock LLM coverage to lock singleton-port behavior</name>
  <files>tests/integration/test_llm_mock.nim</files>
  <action>Add or update mock-backend coverage so one actor-backed owner serializes backend access while the public API remains stable.</action>
  <verify><automated>nim c -r tests/integration/test_llm_mock.nim</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Replace LLM global lock ownership with a singleton port</name>
  <files>src/genex/llm.nim</files>
  <action>Migrate `genex/llm` to the singleton-port substrate from 03-01 and stop using `global_model_lock` / `global_llm_op_lock` as the effective public concurrency boundary.</action>
  <verify><automated>nim c -r tests/integration/test_llm_mock.nim</automated></verify>
</task>
</tasks>

<verification>
- `nim c -r tests/integration/test_llm_mock.nim`
</verification>

<output>
After completion, create `.planning/phases/03-port-actors-for-extensions/03-02-SUMMARY.md`
</output>
